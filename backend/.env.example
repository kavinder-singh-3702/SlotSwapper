# Copy this file to .env and fill in the values before running the app.
PORT=4000
MONGODB_USER=your-username
MONGODB_PASS=your-password
MONGODB_DB=your-database
MONGODB_HOST=cluster.example.mongodb.net
# Optional additional URI options, defaults to retryWrites=true&w=majority if omitted
MONGODB_OPTIONS=
APP_NAME=slotswapper-api
JWT_SECRET=replace-with-secure-random-string
NODE_ENV=development
